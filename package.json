{
  "name": "backendtest",
  "version": "1.0.0",
  "description": "Time it took too complete: `1h 27min` (between first commit `Initial commit 19394231` and `Add test for run() 3c94c958` + like `5min` to add tests for `Direction`)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "test": "jest"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "readline": "^1.3.0"
  }
}
